package com.jyalla.demo;

public class TestWebApp {

    /*
     * @Autowired private WebApplicationContext webApplicationContext;
     * 
     * private MockMvc mockMvc;
     * 
     * @Before(value = "") public void setup() { mockMvc =
     * MockMvcBuilders.webAppContextSetup(webApplicationContext) .build(); }
     * 
     * @Test public void testEmployee() throws Exception { mockMvc.perform(get("/rest/employee"))
     * .andExpect(status().isOk()) .andExpect((ResultMatcher)
     * content().contentType("application/json;charset=UTF-8"))
     * .andExpect(jsonPath("$.name").value("emp1"))
     * .andExpect(jsonPath("$.designation").value("manager"))
     * .andExpect(jsonPath("$.empId").value("1")) .andExpect(jsonPath("$.salary").value(3000));
     * 
     * }
     * 
     * @Test public void whenPostRequestToUsersAndValidUser_thenCorrectResponse() throws Exception {
     * MediaType textPlainUtf8 = new MediaType(MediaType.TEXT_PLAIN, Charset.forName("UTF-8"));
     * String user = "{\"name\": \"bob\", \"email\" : \"bob@domain.com\"}";
     * mockMvc.perform(MockMvcRequestBuilders.post("/users") .content(user)
     * .contentType(MediaType.APPLICATION_JSON_UTF8)) .andExpect(MockMvcResultMatchers.status()
     * .isOk()) .andExpect(MockMvcResultMatchers.content() .contentType(textPlainUtf8)); }
     */

}
